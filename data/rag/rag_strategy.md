# RAG 전략

이 프로젝트에서 RAG 는 외부 비싼 API 없이도 운영할 수 있게 단순하고 안정적인 방식으로 설계한다.
기본 흐름은 다음과 같다.
- 프로젝트 문서를 마크다운으로 저장
- 사용자 질문과 문서의 핵심 키워드를 비교해서 상위 문서를 검색
- 검색된 문서 스니펫을 답변 근거로 함께 제공
- 행사나 마이페이지 질문은 RAG 결과에 더해 실제 API 조회값을 같이 사용

즉, 정적인 설명은 문서에서 가져오고, 실시간 데이터는 Spring Boot API 에서 가져오는 혼합형 RAG 이다.
